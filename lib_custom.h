/* include pour .h*/

#include<stdio.h>
#include<string.h>
#define N 50

/* Définition d'un type strct exemple */
// typedef struct { int jour, mois, an; } t_date;

/* Définition d'un type enum exemple */
//typedef enum { homme = 1, femme } t_sexe;
//
//
/* Définition de toutes les fonctions dans le bon ordre */

//void ajouter_personne(t_personne * amis, int * nb_amis);

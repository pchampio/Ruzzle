#define N 4
#include <stdio.h>

void select_word(){
	int coord_x,coord_;
	printf("quelle est la première case : forme (x y)");
	scanf("%i %i", &coord_x, &coord_y);
	
}

int main(){
	select_word();
	return 0;
}
